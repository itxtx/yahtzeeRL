# Dependencies are declared in pyproject.toml (the single source of truth).
# This file just redirects pip there so the two never drift.
#
#   pip install -r requirements.txt      # runtime + dev deps
#   uv sync --extra dev                  # preferred: exact versions from uv.lock
-e .[dev]
